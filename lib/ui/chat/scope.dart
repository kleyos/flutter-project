import 'package:flutter/material.dart';

class Scope extends StatelessWidget {

  Scope({
    Key key,
  }) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return 
      new Container(
        child: new Center( child: new Text('SCOPE'))
      );
  }
}